
using System;
using System.Linq;
public class MainClass
{
    public static void Main()
    {
      

        int[] cardNumber = Console.ReadLine().Select(x => int.Parse(x.ToString())).ToArray();
        Array.Reverse(cardNumber);
        int min = cardNumber.Min();
        int max = cardNumber.Max();
      int[]  cardNumber2 = new int[16];
        for (int i = 0; i < 14; i++)
            cardNumber2[i] = cardNumber[i];
        cardNumber2[15] = min;
        cardNumber2[14] = max;

        foreach(int x in cardNumber2)
            Console.Write(x);    


    }
}
