using System;

public class MainClass
{
    public static void Main()
    {
        int num = int.Parse(Console.ReadLine());
        int sum = 0;
        for(int i = 0; ; i++)
        {
            sum += i;
            if(sum >= num) break;
            else
            Console.WriteLine(i);
         }
    }
}
