public static void Delta(int[] array)
        {
            for (int i = 0; i < array.Length; i++)
            {
                if(i==0)
                    Console.Write(array[i] + " ");
                else
                Console.Write(array[i] - array[i-1] + " ");
            }
           
                
        }
